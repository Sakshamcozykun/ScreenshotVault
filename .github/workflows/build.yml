name: Build ScreenshotVault

on:
  push:
    # Added 'master' just in case your default branch isn't named 'main'
    branches: [ main, master ] 
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Publish App
      # dotnet publish handles restore, build, and routing files to the output folder automatically
      run: >
        dotnet publish src/ScreenshotVault.App/ScreenshotVault.App.csproj 
        -c Release 
        -r win-x64 
        --self-contained true 
        -p:WindowsPackageType=None 
        -o ./publish

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ScreenshotVault-exe
        path: ./publish/
        retention-days: 30